<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.3.2</version>
    <relativePath/>
  </parent>

  <groupId>com.acme</groupId>
  <artifactId>inventory-service</artifactId>
  <version>1.0.0</version>
  <name>inventory-service</name>
  <description>Spring Boot 3 / Java 21 modernization of legacy InventoryServlet</description>

  <properties>
    <java.version>21</java.version>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>

    <!--
      Compile scope, not runtime: the swagger annotations on the controller and the application
      class are compiled against it. It is third-party, so spring-boot-starter-parent does NOT
      manage its version and an explicit one is required - 2.6.0 pairs with Boot 3.3.x.
      It needs no other wiring: /v3/api-docs and /swagger-ui.html are served at their defaults.
    -->
    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
      <version>2.6.0</version>
    </dependency>

    <!--
      Runtime, not test: a test-scoped driver is absent from the packaged jar and the app
      dies at startup with "Failed to configure a DataSource". Runtime scope is on the test
      classpath too, so the integration tests below still see it.
    -->
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>

    <!-- Test -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!--
      Testcontainers gives the *IT classes a real PostgreSQL rather than an embedded stand-in,
      so the schema.sql the service actually ships is the one the tests run against.
      spring-boot-testcontainers supplies @ServiceConnection, which points the context at the
      container without a single hardcoded URL. Every version here is managed by
      spring-boot-starter-parent (it imports testcontainers-bom) - do not pin them.
    -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-testcontainers</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>postgresql</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>

      <!--
        Runs the *IT classes. Version and the integration-test/verify goal bindings both come
        from spring-boot-starter-parent's pluginManagement, so a bare declaration is enough.
        Declared BEFORE pitest deliberately: failsafe:verify and pitest:mutationCoverage are
        both bound to the `verify` phase, and Maven runs same-phase goals in declaration
        order - so a broken integration test reports immediately instead of after a
        multi-minute mutation run.
      -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
      </plugin>

      <plugin>
        <groupId>org.pitest</groupId>
        <artifactId>pitest-maven</artifactId>
        <version>1.15.8</version>
        <dependencies>
          <dependency>
            <groupId>org.pitest</groupId>
            <artifactId>pitest-junit5-plugin</artifactId>
            <version>1.2.1</version>
          </dependency>
        </dependencies>
        <configuration>
          <mutationThreshold>80</mutationThreshold>
          <threads>4</threads>
          <targetClasses>
            <param>com.acme.inventory.*</param>
            <param>com.acme.inventory.*.*</param>
          </targetClasses>
          <targetTests>
            <param>com.acme.inventory.*</param>
            <param>com.acme.inventory.*.*</param>
          </targetTests>
          <!--
            PIT finds test classes on the classpath, not from surefire's configuration, so
            without this it would run the *IT classes inside its own forked JVMs - a
            PostgreSQL container per mutant. Worse than slow: PIT counts a TIMED_OUT mutant
            as detected, so container-bound tests could inflate the score rather than earn
            it. The gate is a unit-test gate; integration tests are failsafe's job.
            One param is enough: PIT compiles a glob's `*` to regex `.*`, which spans the
            package dots, so this also matches com.acme.inventory.repository.*IT.
          -->
          <excludedTestClasses>
            <param>com.acme.inventory.*IT</param>
          </excludedTestClasses>
          <outputFormats>
            <param>HTML</param>
            <param>XML</param>
          </outputFormats>
          <failWhenNoMutations>true</failWhenNoMutations>
        </configuration>
        <executions>
          <execution>
            <id>pitest</id>
            <phase>verify</phase>
            <goals>
              <goal>mutationCoverage</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
